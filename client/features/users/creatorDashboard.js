import React from "react";
// import fetchLessonsAsync

const CreatorDashboard = () => {
  return (
    <div>
      <h1>Creator Dashboard</h1>
      {/* First section - Maps through all lessons, filters by "published = true"
      - Published lessons
      Second section - Maps through all lessons, filters
      by "published = false" - Drafts */}
    </div>
  );
};

export default CreatorDashboard;
